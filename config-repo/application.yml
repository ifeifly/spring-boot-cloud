ribbon:
  ReadTimeout: 5000
  ConnectTimeout: 5000

logging:
  level:
    root: debug

feign:
  hystrix:
    enabled: true

sys:
  config:
    value: default789

spring:
  cloud:
    kubernetes:
      ribbon:
        mode: service
      discovery:
        enabled: true
      client:
        namespace: default
